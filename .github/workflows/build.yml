name: Build desktop apps

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-windows:
    name: Build Windows
    runs-on: windows-latest
    timeout-minutes: 45

    env:
      CSC_IDENTITY_AUTO_DISCOVERY: false

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type-check
        run: npm run typecheck

      - name: Prepare and verify offline Chinese speech
        run: |
          npm run prepare:speech
          npm run test:speech
        env:
          SPEECH_SMOKE_NATIVE: '1'

      - name: Package application
        run: npm run build:win

      - name: Verify packaged speech resources
        run: node node_modules/.cache/speech-server-smoke.cjs
        env:
          SPEECH_SMOKE_NATIVE: '1'
          SPEECH_SMOKE_RESOURCES: dist/win-unpacked/resources

      - name: Upload installers
        uses: actions/upload-artifact@v7
        with:
          name: ai-agent-management-windows
          path: |
            dist/*.exe
            dist/*.blockmap
            dist/*.yml
          if-no-files-found: error
          retention-days: 14
